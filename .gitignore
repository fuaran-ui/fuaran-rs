# Rust build artifacts
/target/

# The built wasm32 client module (a target/ artefact); if copied beside the
# demo page for local serving, keep it out of the repo.
*.wasm

# Cargo.lock is not committed for a library crate (this host is a library).
Cargo.lock

# Editors / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Synced corpus snapshot (follow-up: offline snapshot + drift guard)
tests/corpus/

# Phase 537 native-mobile packaging outputs (assembled by run.ps1 -Package from
# the cross-target build legs; regenerated, not source). The Swift/Kotlin scaffold
# source lands in later phases and will be re-included explicitly.
/packaging/
