{
  "version": 1,
  "host": "fuaran-rs",
  "family": "pre-emit",
  "vocabulary": "wire-format-fixtures/validator/defect-vocabulary.json",
  "posture": "subset",
  "postureReason": "The widest sibling coverage, and the only one carrying the code as a first-class value on the finding rather than inside the message prose — which is why it is the only sibling whose declaration can be machine-checked against its implementation.",
  "implemented": [
    "FUARAN047",
    "FUARAN048",
    "FUARAN049",
    "FUARAN069",
    "FUARAN073",
    "FUARAN082",
    "FUARAN083",
    "FUARAN086",
    "FUARAN087",
    "FUARAN088",
    "FUARAN089",
    "FUARAN097",
    "FUARAN108"
  ],
  "abstentionDefault": "Not ported. This is an honest 'not yet', not a principled exclusion: the set above is whichever rules this host happened to need, and an unported rule is a gap.",
  "abstained": {
    "FUARAN106": "Not ported, and named rather than left to the default because this host DID adopt the §24.4 seeding rule the code guards (`src/render/seeds.rs`) — so silence here would read as \"nothing to say\" when the truth is a deliberate split between the semantics and the diagnosis. The renderer takes the FIRST declaration in tree order and is deterministic about it, exactly as the reference does; what is missing is the validator that tells an author the second declaration was discarded. The rule quantifies over EVERY `Binding.State` declaration in the tree and compares their values, which needs the cross-tree binding walk this single-pass node walker does not have — the same missing projection FUARAN105 names, and the same prerequisite. A partial walk is worse here than none: it would see one of two conflicting declarations, find no disagreement and report nothing, which an author reads as agreement. Note the seeding walk that DOES reach every binding is deliberately structural over the tree's canonical JSON rather than typed, and it is a value-collector rather than a finding-producer: it has no node id to anchor a finding to, which is the second half of what this rule needs.",
    "FUARAN107": "Not ported — same missing walk, plus one requirement of its own. Beyond reaching every binding, the rule must decide that a row-major grid `source` and a columnar `Transform` source are the SAME table, which means normalising two spellings of one value before comparing them. This host has that normalisation (`normalise_transform_source`, `transpose_row_major`) but only inside the decoder, on raw JSON, not as a projection over a decoded tree — so the comparison the rule needs has no operands here yet. Abstained rather than approximated: a version comparing the two spellings literally would call every idiomatic pair distinct and report nothing, which is the same silent pass FUARAN106's partial walk would give.",
    "FUARAN105": "Not ported, and named here rather than left to the default because Phase 870 considered it against this host's actual validator and declined on evidence. The rule reasons from the ABSENCE of a writer ANYWHERE IN THE TREE ('nothing in the tree writes the key'), which needs two projections this host does not have: a tree-wide State-WRITE set, and a binding traversal that descends a Transform's own source slot. This validator is a single-pass node walker — `Walker` carries findings and a duplicate-id count and nothing else — and inspects bindings only where a named rule reaches into a named slot. That is not an accident of coverage: FUARAN098 (SetStateNoReader) is the rule whose adoption would have BUILT the write projection, and it is unported here too, so there is nothing to reuse. Porting FUARAN105 without it would not merely under-report — with no write projection every key reads as unwritten, so the rule would fire on every tree that legitimately writes one, which is a false accusation on an idiomatic shape. The prerequisite is the cross-tree binding walk, which is its own work. Same conclusion, same reason and the same 669 mechanism as fuaran-ts's declared abstention; note the 865 ship recorded a related latent gap on the reference side (its `usesOfBinding` does not descend a Transform's source slot either), so the projection a port would need is not fully settled upstream."
  },
  "otherFamilies": {
    "ast-walker": [
      "FUARAN001",
      "FUARAN050",
      "FUARAN061",
      "FUARAN063",
      "FUARAN064",
      "FUARAN084"
    ]
  },
  "otherFamiliesNote": "Codes this host raises that belong to the reference's BUILD-TIME source-AST walker, not the pre-emit family enumerated in the vocabulary. Recorded so the gate reads them as a different family rather than as a code this host invented. That second family is not yet enumerated; until it is, this list is a declaration of intent, not something the gate can verify.",
  "machineChecked": true,
  "machineCheckedNote": "This host carries the code as a value on the finding, so `implemented` is checkable against the validator source. The check is not yet wired — the gate verifies the declaration against the vocabulary only."
}
